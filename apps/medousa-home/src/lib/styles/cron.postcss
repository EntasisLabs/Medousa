/**
 * Cron / recurring automations panel.
 * Loaded by the destination feature entry; not imported from app.postcss.
 */

@layer features {


  .cron-panel-mobile {
    @apply overflow-x-hidden;
  }

  .cron-panel-mobile .workshop-header {
    @apply py-2;
  }

  .cron-panel-mobile .workshop-header-line {
    @apply hidden;
  }

  .cron-panel-mobile .cron-create-form {
    @apply space-y-3 pb-6;
  }

  .cron-panel-mobile .cron-field-label {
    @apply mb-1 text-[11px] normal-case tracking-normal text-content-tertiary;
  }

  .cron-panel-mobile .cron-field-bar {
    @apply rounded-xl;
  }

  .cron-panel-mobile .cron-field-bar-compact {
    @apply py-1;
  }

  .cron-panel-mobile aside,
  .cron-panel-mobile .workshop-detail-pane {
    @apply px-3 py-3;
  }

  .friendly-schedule {
    @apply space-y-3;
  }

  .friendly-schedule-summary {
    @apply text-sm leading-snug text-surface-200;
  }

  .friendly-schedule-frequency {
    @apply flex flex-wrap gap-2;
  }

  .friendly-schedule-chip {
    @apply rounded-full border border-surface-500/40 bg-surface-900/40 px-3 py-1.5 text-xs text-content-secondary transition active:scale-[0.98];
  }

  .friendly-schedule-chip-active {
    @apply border-primary-500/45 text-surface-50;
    background-color: rgb(var(--color-primary-500) / 0.12);
  }

  .friendly-schedule-row {
    @apply grid gap-3 sm:grid-cols-2;
  }

  .friendly-schedule-field {
    @apply block min-w-0;
  }

  .friendly-schedule-field-label {
    @apply mb-1 block text-[11px] text-content-tertiary;
  }

  .friendly-schedule-hint {
    @apply mt-1 text-[10px] leading-snug text-content-quiet;
  }

  .friendly-schedule-mobile .friendly-schedule-chip {
    @apply py-2 text-[13px];
  }

  /* Cron — composer-style fields; 16px inputs on mobile prevent iOS focus zoom */
  .cron-create-form {
    @apply mt-3 space-y-4;
  }

  .cron-field-label {
    @apply mb-1.5 block text-[10px] uppercase tracking-wide text-content-quiet;
  }

  .cron-field-bar {
    @apply rounded-[14px];
  }

  .cron-field-bar-compact {
    @apply px-2.5 py-0.5;
  }

  .cron-field-input,
  .cron-search-input {
    @apply min-h-[36px] w-full border-0 bg-transparent py-2 pl-1 pr-1 text-base leading-snug text-surface-50 shadow-none placeholder:text-content-tertiary focus:border-0 focus:outline-none focus:ring-0;
    -webkit-font-smoothing: antialiased;
  }

  .cron-search-bar {
    @apply rounded-[14px] px-2.5 py-0.5;
  }

  .cron-search-input {
    @apply pl-2;
  }

  .cron-field-row {
    @apply grid gap-3 sm:grid-cols-[minmax(0,1.6fr)_minmax(0,1fr)];
  }

  .cron-field-hint {
    @apply text-xs text-content-quiet;
  }

  .cron-form-actions {
    @apply flex flex-wrap gap-2 border-t border-surface-500/30 pt-4;
  }

  .cron-panel-mobile .cron-create-form {
    @apply pb-8;
  }

  .cron-panel-mobile .cron-form-actions {
    @apply flex-col gap-2.5;
  }

  .cron-panel-mobile .cron-form-submit {
    @apply w-full;
  }
}

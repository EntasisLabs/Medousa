[package]
name = "medousa-computer-bridge"
version = "0.10.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Platform-neutral computer driver contract for governed Medousa worlds"

[dependencies]
medousa-world = { path = "../medousa-world" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
